(((esi <- 7)
  (eax <- (allocate esi 3))
  (edi <- eax)
  ((mem edi 8) <- 25)
  (ebx <- (mem edi 4))
  (eax <- (print ebx))
  (ebx <- (mem edi 8))
  (eax <- (print ebx))
  (ebx <- (mem edi 12))
  (eax <- (print ebx))))