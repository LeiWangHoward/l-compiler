(((esp -= 4)
  ((mem ebp -4) <- eax) 
  (eax <- 3)
  (eax <- 7)
  (ebx <- 7)
  (ecx <- 7)
  (edx <- 7)
  (esi <- 7)
  (edi <- 7)
  ((mem ebp -4) <- eax)
  (eax <- 0)
  (eax <- (mem ebp -4))
  (esp += 4)
  (eax += ebx)
  (eax += ecx)
  (eax += edx)
  (eax += esi)
  (eax += edi)
  (eax <- eax)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax))))

