(((eax <- 3)
  (call :fun1)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax)))
 (:fun1
  (esp -= 8)
  ((mem ebp -4) <- 7)
  ((mem ebp -8) <- 9)
  (call :fun2)
  (eax += 2)
  (call :fun3)
  ((mem ebp -4) <- eax)
  (ebx <- (mem ebp -4))
  (ecx <- (mem ebp -8))
  (eax <- ebx)
  (eax += ecx)
  (return))
 (:fun2
  (eax += eax)
  (eax += 1)
  (return))
 (:fun3
  (eax += eax)
  (eax += 3)
  (call :fun4)
  (return))
 (:fun4
  (return)))
