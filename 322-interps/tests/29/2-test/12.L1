(((eax <- 10)
  (call :f)
  (eax += eax)
  (eax += 1)
  (eax <- (print eax)))
 (:f
  (esp -= 4)
  ((mem ebp -4) <- 1)
  (call :g)
  (ebx <- (mem ebp -4))
  (eax += ebx)
  (esp += 4)
  (return))
 (:g
  (esp -= 4)
  ((mem ebp -4) <- 2)
  (ebx <- (mem ebp -4))
  (eax += ebx)
  (esp += 4)
  (return)))
