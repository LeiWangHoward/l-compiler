((((mem ebp -12) <- 1)
  (ebx <- 2)
  (ecx <- 3)
  (edx <- 4)
  (eax <- (mem ebp -12))
  (eax <- eax < ebx)
  ((mem ebp -4) <- eax)
  (eax <- ecx < edx)
  ((mem ebp -8) <- eax)
  (eax <- (mem ebp -12))
  (eax += ebx)
  ((mem ebp -12) <- eax)
  (ecx += edx)
  (eax <- (mem ebp -4))
  (ebx <- (mem ebp -8))
  (eax += ebx)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (ebx <- (mem ebp -12))
  (eax += ebx)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (eax += ecx)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (eax += 1)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (eax <- (print eax))))