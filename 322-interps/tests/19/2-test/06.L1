(((call :fun))
 (:fun
  (edx <- 0)
  ((mem ebp -4) <- 1)
  ((mem ebp -8) <- 2)
  (ecx <- 3)
  (ebx <- 4)
  (eax <- (mem ebp -4))
  (edx += eax)
  (eax <- (mem ebp -8))
  (edx += eax)
  (edx += ecx)
  (edx += ebx)
  (edx += 1)
  (eax <- (print edx))
  (return)))