(((ecx <- 2)
  ((mem ebp -4) <- 3)
  (eax <- (mem ebp -4))
  (eax <<= ecx)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (eax += 1)
  ((mem ebp -4) <- eax)
  (eax <- (mem ebp -4))
  (eax <- (print eax))))