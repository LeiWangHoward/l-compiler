(((esi <- 0)
  ((mem ebp -4) <- 1)
  ((mem ebp -8) <- 2)
  (edx <- 3)
  (edi <- 4)
  (eax <- 5)
  (ebx <- 6)
  (ecx <- (mem ebp -4))
  (esi += ecx)
  (ecx <- (mem ebp -8))
  (esi += ecx)
  (esi += edx)
  (esi += edi)
  (esi += eax)
  (esi += ebx)
  (eax <- (print esi))))