(((call :fun))
  (:fun (ecx <- 3)
  	    (edx <- ecx)
  	    (eax <- (print edx))
  	    (call :fun2)
  	    (return))
  (:fun2 (ecx <- 7)
  		 (edx <- ecx)
  		 (eax <- (print edx))
  	     (return)))