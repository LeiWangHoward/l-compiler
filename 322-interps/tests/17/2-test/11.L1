(((call :f)
  (eax <- (print 7)))
 (:f
  (esp -= 4)
  (eax <- (print 5))
  (ebx <- 47)
  ((mem ebp -4) <- 3)
  (ecx <- (mem ebp -4))
  (ebx <<= ecx)
  (ebx += 1)
  (eax <- (print ebx))
  (ecx <- :g)
  (eax <- (mem ebp -4))
  (tail-call ecx))
 (:g
  (esp -= 4)
  ((mem ebp -4) <- edi)
  (edi <- eax)
  (eax <- (print edi))
  (ebx <- edi < 9)
  (ebx *= 2)
  (ebx += 1)
  (eax <- (print ebx))
  (edi <- (mem ebp -4))
  (return)))