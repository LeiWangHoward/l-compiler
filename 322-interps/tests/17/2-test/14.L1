(((esp -= 8)
  (eax <- 7)
  (ebx <- 7)
  (ecx <- 7)
  (edx <- 7)
  (edi <- 7)
  ((mem ebp -8) <- 7)
  (esi <- eax)
  (esi += ebx)
  (esi += ebx)
  (esi += ecx)
  (esi += edx)
  (esi += edi)
  (ebx <- (mem ebp -8))
  (esi += ebx)
  (esi += eax)
  (esi += 1)
  (eax <- esi)
  (eax <- (print eax))
  (esp += 8)))