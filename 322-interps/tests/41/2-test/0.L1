(((call :foo)
  (eax <- (print eax)))
 (:foo
  ((mem ebp -16) <- esi)
  (ebx <- edi)
  ((mem ebp -8) <- 1)
  ((mem ebp -12) <- 1)
  (ecx <- 1)
  (edx <- 1)
  (eax <- 1)
  (esi <- 1)
  ((mem ebp -4) <- 1)
  (edi <- (mem ebp -8))
  (edi += edi)
  ((mem ebp -8) <- edi)
  (edi <- (mem ebp -12))
  (edi += edi)
  ((mem ebp -12) <- edi)
  (ecx += ecx)
  (edx += edx)
  (eax += eax)
  (esi += esi)
  (ecx <- (mem ebp -4))
  (ecx += ecx)
  ((mem ebp -4) <- ecx)
  (ecx <- (mem ebp -4))
  (ecx += 1)
  ((mem ebp -4) <- ecx)
  (eax <- (mem ebp -4))
  (esi <- (mem ebp -16))
  (edi <- ebx)
  (return)))
