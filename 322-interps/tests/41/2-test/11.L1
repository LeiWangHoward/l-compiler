(((call :foo)
  (eax += 1)
  (eax <- (print eax)))
 (:foo
  (esp -= 12)
  ((mem ebp -4) <- edi)
  ((mem ebp -8) <- 5)
  (ebx <- 99)
  ((mem ebp -12) <- 3)
  (ecx <- (mem ebp -8))
  (ebx >>= ecx)
  (edx <- (mem ebp -8))
  (ecx <- (mem ebp -12))
  (edx <<= ecx)
  ((mem ebp -8) <- edx)
  (goto :meh)
  :meh
  (eax <- edx)
  (edi <- (mem ebp -4))
  (return)))
