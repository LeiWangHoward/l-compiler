(((call :foo)
  (eax += 1)
  (eax <- (print eax)))
 (:foo
  (eax <- 1)
  (ebx <- 1)
  (ecx <- 1)
  (edx <- 1)
  (esp -= 4)
  ((mem ebp -4) <- 1)
  (eax += eax)
  (ebx += ebx)
  (ecx += ecx)
  (edx += edx)
  (ecx <- (mem ebp -4))
  (ecx += ecx)
  ((mem ebp -4) <- ecx)
  (return)))
