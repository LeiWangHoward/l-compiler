(((eax <- 1)
  (call :foo)
  (eax <- (print eax)))
 (:foo
  (esp -= 12)
  ((mem ebp -4) <- esi)
  ((mem ebp -8) <- edi)
  ((mem ebp -12) <- 3)
  (edx <- (mem ebp -12))
  (ecx <- edx)
  (ecx <- ecx)
  (ebx <- (mem ebp -12))
  (edx <- edx)
  (edx <- ebx)
  (ecx <- ecx)
  (edx <- (mem ebp -12))
  (ecx += edx)
  (esi <- (mem ebp -4))
  (edi <- (mem ebp -8))
  (return)))
