(((eax <- 17)
  (call :foo)
  (eax <- (print eax)))
 (:foo
  (esp -= 4)
  ((mem ebp -4) <- esi)
  (ebx <- 4)
  (ecx <- 5)
  (edx <- 7)
  (esi <- 3)
  (esi += eax)
  (esi += ebx)
  (esi += ecx)
  (esi += ebx)
  (esi += ecx)
  (esi += edx)
  (esi += eax)
  (esi += edx)
  (esi <- (mem ebp -4))
  (return)
  ))
