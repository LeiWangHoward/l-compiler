(((ebx <- 3)
  (esi <- 5)
  (eax <- ebx)
  (eax += esi)
  (call :pass)
  (eax <- (print ebx))
  (eax <- (print esi)))
 (:pass
  (esp -= 4)
  ((mem ebp -4) <- edi)
  (eax += eax)
  (edi <- eax)
  (edi += 1)
  (eax <- (print edi))
  (edi <- (mem ebp -4))
  (return)))
