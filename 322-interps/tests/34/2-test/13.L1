(((call :add_up))
 (:add_up
  (esp += -12)
  (ebx <- 3)
  (eax <- 5)
  ((mem ebp -4) <- 7)
  ((mem ebp -8) <- 9)
  ((mem ebp -12) <- 11)
  (ebx += eax)
  (ebx -= 1)
  (eax <- (print ebx))
  (ecx <- (mem ebp -4))
  (ebx += ecx)
  (ebx -= 1)
  (eax <- (print ebx))
  (ecx <- (mem ebp -8))
  (ebx += ecx)
  (ebx -= 1)
  (eax <- (print ebx))
  (ecx <- (mem ebp -12))
  (ebx += ecx)
  (ebx -= 1)
  (eax <- (print ebx))
  (return)))
