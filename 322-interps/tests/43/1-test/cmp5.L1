((
    (eax <- 3)
    (ebx <- 5 < eax)
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))
    
    (eax <- 3)
    (ebx <- eax < 5)
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))

    (eax <- 3)
    (ebx <- eax <= 5)
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))

    (eax <- 3)
    (ebx <- 5 = eax)
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))

    (eax <- 3)
    (ebx <- eax = eax)
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))
))
