((
    (eax <- (allocate 9 3))
    (edi <- eax)
    (ebx <- (mem edi 0))
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print edi))
    (edx <- 7)
    ((mem edi 4) <- edx)
    (ebx <- (mem edi 4))
    (ebx *= 2)
    (ebx += 1)
    (eax <- (print ebx))
))

